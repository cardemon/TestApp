# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Technical task iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire'
  pod 'RxCocoa',    '~> 4.0'
  pod 'RxSwift',    '~> 4.0'
  pod 'AlamofireImage', '~> 3.5'

  # Pods for Technical task iOS

  target 'Technical task iOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Technical task iOSUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
